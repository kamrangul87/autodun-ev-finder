# Python dependencies for Autodun Nexus data+ML pipeline
# Note: NOT required for standard Next.js deployment (npm run build / npm start)
# Only needed when running the full stack pipeline locally with: bash run.sh

pandas==2.2.2
pyarrow==16.1.0
duckdb==1.0.0
requests==2.32.3
dbt-core==1.7.14
dbt-duckdb==1.7.4
fastapi==0.115.0
uvicorn==0.30.6
scikit-learn==1.5.2
