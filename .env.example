# Autodun EV Finder - Environment Variables

# ============================================
# REPLIT ENVIRONMENT (use Replit Secrets)
# ============================================

# OpenChargeMap API Key - Get yours at https://openchargemap.org/site/profile/register
OCM_API_KEY=your_ocm_api_key_here

# Data Source Mode: "OPENCHARGE" or "OCM" for live data, "STATIC" for JSON file, "DEMO" for demo data
STATIONS=OPENCHARGE

# ============================================
# VERCEL ENVIRONMENT (set in Vercel Dashboard)
# ============================================
# Go to: Project Settings → Environment Variables
# Add these for both Preview and Production deployments:

# Data Source Mode (use STATIONS_SOURCE for Vercel)
STATIONS_SOURCE=OPENCHARGE

# OpenChargeMap API Key (same as above)
# OCM_API_KEY=your_ocm_api_key_here

# Optional: Client identifier for OCM API tracking
# OCM_CLIENT=autodun-ev-finder

# ============================================
# OPTIONAL (both platforms)
# ============================================

# Custom map tile server URL (defaults to OpenStreetMap)
# NEXT_PUBLIC_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png

# Custom council boundary data URL (falls back to local file)
# COUNCIL_DATA_URL=https://example.com/councils.geojson

# ============================================
# NOTES
# ============================================
# - API uses maxresults=1000 by default for viewport fetching
# - /api/stations is runtime-dynamic (force-dynamic) with no caching
# - Fetch requests use cache: 'no-store' to prevent stale data
# - Response headers disable HTTP caching for always-fresh results
