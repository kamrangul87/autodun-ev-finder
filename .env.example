# Open Charge Map API Configuration
# Get your API key from: https://openchargemap.org/site/develop/api

# Your Open Charge Map API key
OCM_API_KEY=your_api_key_here

# Data source configuration
# Options: DEMO (sample data), OPENCHARGE (live OCM data), STATIC (local static data)
STATIONS=DEMO

# Optional: Council data URL (falls back to local file then empty FeatureCollection)
COUNCIL_DATA_URL=

# Optional: Custom map tile URL (uses OSM default if not set)
NEXT_PUBLIC_TILE_URL=
