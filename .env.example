# Station data source: DEMO | OPENCHARGEMAP | LOCAL
STATIONS=DEMO

# Optional: Council data URL (falls back to local file then empty FeatureCollection)
COUNCIL_DATA_URL=

# Optional: Custom map tile URL (uses OSM default if not set)
NEXT_PUBLIC_TILE_URL=
